/**
 * Bridge branding configuration.
 * Edit APP_NAME here to customize the app name.
 */
export const APP_NAME   = process.env.BRIDGE_APP_NAME ?? 'Agentory';
export const APP_ID     = `com.agentory.bridge`;
export const BRIDGE_NAME = `${APP_NAME} Bridge`;
